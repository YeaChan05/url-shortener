package org.yechan.mcp.repository.exposed

/*
import org.jetbrains.exposed.sql.Table
import org.jetbrains.exposed.sql.javatime.datetime
import java.time.LocalDateTime

object UrlMappings : Table() {
    val id = integer("id").autoIncrement()
    val shortKey = varchar("short_key", length = 10).uniqueIndex()
    val originalUrl = text("original_url")
    val createdAt = datetime("created_at").default(LocalDateTime.now())
    val clickCount = integer("click_count").default(0)
    
    override val primaryKey = PrimaryKey(id)
}
*/

// 이 파일은 더 이상 사용되지 않습니다.
// 프로젝트를 단순화하기 위해 내용을 주석 처리했습니다.
